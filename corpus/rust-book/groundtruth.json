{
  "slug": "rust-book",
  "framework": "mdBook",
  "final_url": "https://doc.rust-lang.org/book/ch08-01-vectors.html",
  "html_bytes": 38648,
  "content_root": "main",
  "thresholds": {
    "MIN_CODE_CHARS": 24,
    "MIN_HEADING_CHARS": 3,
    "BOILERPLATE_MIN": 12,
    "BOILERPLATE_MAX": 100,
    "BOILERPLATE_CAP": 80,
    "USABLE_MIN_CODE": 3,
    "USABLE_MIN_HEADINGS": 5
  },
  "usable": true,
  "unusable_reason": null,
  "content_chars": 10717,
  "body_chars": 10919,
  "content_ratio": 0.9815,
  "article_in_server_html": true,
  "server_html_note": null,
  "counts": {
    "code_blocks": 11,
    "headings": 7,
    "paragraphs": 29,
    "boilerplate": 2,
    "boilerplate_dropped_by_cap": 0,
    "tables": 0
  },
  "code_blocks": [
    {
      "probe": "fnmain(){letv:Vec<i32>=Vec::new();}",
      "length": 35
    },
    {
      "probe": "fnmain(){letv=vec![1,2,3];}",
      "length": 27
    },
    {
      "probe": "fnmain(){letmutv=Vec::new();v.push(5);v.",
      "length": 69
    },
    {
      "probe": "fnmain(){letv=vec![1,2,3,4,5];letthird:&",
      "length": 222
    },
    {
      "probe": "fnmain(){letv=vec![1,2,3,4,5];letdoes_no",
      "length": 86
    },
    {
      "probe": "fnmain(){letmutv=vec![1,2,3,4,5];letfirs",
      "length": 97
    },
    {
      "probe": "$cargorunCompilingcollectionsv0.1.0(file",
      "length": 451
    },
    {
      "probe": "fnmain(){letv=vec![100,32,57];foriin&v{p",
      "length": 57
    },
    {
      "probe": "fnmain(){letmutv=vec![100,32,57];foriin&",
      "length": 54
    },
    {
      "probe": "fnmain(){enumSpreadsheetCell{Int(i32),Fl",
      "length": 176
    },
    {
      "probe": "fnmain(){{letv=vec![1,2,3,4];//dostuffwi",
      "length": 78
    }
  ],
  "headings": [
    "storing lists of values with vectors",
    "creating a new vector",
    "updating a vector",
    "reading elements of vectors",
    "iterating over the values in a vector",
    "using an enum to store multiple types",
    "dropping a vector drops its elements"
  ],
  "paragraphs": [
    "the first collection type we ll look at is vec t also known as a vector vectors allow you to store more than one value i",
    "to create a new empty vector we call the vec new function as shown in listing 8 1",
    "note that we added a type annotation here because we aren t inserting any values into this vector rust doesn t know what",
    "more often you ll create a vec t with initial values and rust will infer the type of value you want to store so you rare",
    "because we ve given initial i32 values rust can infer that the type of v is vec i32 and the type annotation isn t necess",
    "to create a vector and then add elements to it we can use the push method as shown in listing 8 3",
    "as with any variable if we want to be able to change its value we need to make it mutable using the mut keyword as discu",
    "there are two ways to reference a value stored in a vector via indexing or by using the get method in the following exam",
    "listing 8 4 shows both methods of accessing a value in a vector with indexing syntax and the get method",
    "note a few details here we use the index value of 2 to get the third element because vectors are indexed by number start",
    "rust provides these two ways to reference an element so that you can choose how the program behaves when you try to use ",
    "when we run this code the first method will cause the program to panic because it references a nonexistent element this ",
    "when the get method is passed an index that is outside the vector it returns none without panicking you would use this m",
    "when the program has a valid reference the borrow checker enforces the ownership and borrowing rules covered in chapter ",
    "compiling this code will result in this error",
    "the code in listing 8 6 might look like it should work why should a reference to the first element care about changes at",
    "note for more on the implementation details of the vec t type see the rustonomicon",
    "to access each element in a vector in turn we would iterate through all of the elements rather than use indices to acces",
    "we can also iterate over mutable references to each element in a mutable vector in order to make changes to all the elem",
    "to change the value that the mutable reference refers to we have to use the dereference operator to get to the value in ",
    "iterating over a vector whether immutably or mutably is safe because of the borrow checker s rules if we attempted to in",
    "vectors can only store values that are of the same type this can be inconvenient there are definitely use cases for need",
    "for example say we want to get values from a row in a spreadsheet in which some of the columns in the row contain intege",
    "rust needs to know what types will be in the vector at compile time so that it knows exactly how much memory on the heap",
    "if you don t know the exhaustive set of types a program will get at runtime to store in a vector the enum technique won ",
    "now that we ve discussed some of the most common ways to use vectors be sure to review the api documentation for all of ",
    "like any other struct a vector is freed when it goes out of scope as annotated in listing 8 10",
    "when the vector gets dropped all of its contents are also dropped meaning the integers it holds will be cleaned up the b",
    "let s move on to the next collection type string"
  ],
  "boilerplate": [
    "the rust programming language",
    "keyboard shortcuts"
  ],
  "tables": 0
}